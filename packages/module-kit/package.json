{
  "name": "@claudeshop/module-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Plugin SDK — manifest Zod schema, lifecycle interface, context factory",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@claudeshop/contracts": "workspace:*",
    "@claudeshop/events": "workspace:*",
    "@claudeshop/telemetry": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@claudeshop/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
